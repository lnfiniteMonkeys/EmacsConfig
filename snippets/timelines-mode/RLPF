# -*- mode: snippet -*-
# name: RLPF
# key: RLPF
# --
synth "${1:name}_RLPF" $ do
  "amp" <>< let
    in ${2:0}
  "freq" <>< let
    in ${3:beatDur}
  "rq" <>< let
    in ${4:0.1}
  "pan" <>< let
    in ${0:0}
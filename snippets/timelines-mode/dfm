# -*- mode: snippet -*-
# name: DFM
# key: DFM
# --
synth "${1:name}_DFM" $ do
  "amp" <>< let
    in ${2:0}
  "freq" <>< let
    in ${3:beatDur}
  "res" <>< let
    in ${4:0.1}
  "gain" <>< let
    in ${4:0.5}
  "pan" <>< let
    in ${0:0}
# -*- mode: snippet -*-
# name: lpfnoise
# key: lpfnoise
# --
synth "$1_noiseLPFdelay" $ do
  "amp" <>< let
    in ${2:0}
  "cutoff" <>< let
    in ${3:1000}
  "resonance" <>< let
    in ${4:1}
  "delayTime" <>< let
    in ${5:0}
  "decayTime" <>< let
    in ${6:0}
  "pan" <>< let
    in ${0:0}